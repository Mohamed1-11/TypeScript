let firstNumber: number = 10;
let secondNumber: number = 5;
let equalvalue = firstNumber == secondNumber; // check valuae only
let equalValueaAnddatatype = firstNumber === secondNumber; // check value and type
let notEqual = firstNumber != secondNumber; // not equal value
let notequalValueaAnddatatype = firstNumber !== secondNumber; // not equal value or type
console.log("Is first number equal to second number? " + equalvalue);
// equalvalue IS BOOLEN TYPE BY DEFAULT BC ITS COMPARISON OPERATOR (==, ===, !=, !==)
firstNumber > secondNumber
firstNumber < secondNumber
firstNumber >= secondNumber
firstNumber <= secondNumber
let itstrue = true
let istfalse = false
let isAnd = itstrue && istfalse;  // AND operator 
let isOr = itstrue || istfalse;   // OR operator
console.log("AND operator result: " + isAnd);
console.log("OR operator result: " + isOr);



